# 📕 Задание
Напишите на C# или Python библиотеку для поставки внешним клиентам, которая умеет вычислять площадь круга по радиусу и треугольника по трем сторонам. Дополнительно к работоспособности оценим:
- Юнит-тесты
- Легкость добавления других фигур
- Вычисление площади фигуры без знания типа фигуры в compile-time
- Проверку на то, является ли треугольник прямоугольным

## 🚀 Возможности

- ✅ Вычисление площади круга по радиусу  
- ✅ Вычисление площади треугольника по трём сторонам  
- ✅ Проверка, является ли треугольник прямоугольным  
- ✅ Легкое добавление новых фигур  
- ✅ Вычисление площади **без знания типа фигуры в compile-time** (через полиморфизм)  
- ✅ Покрытие юнит-тестами (`pytest`)

## ⚙️ Добавление новых фигур
#### Чтобы добавить новую фигуру (например, квадрат):
1. Создать класс, унаследованный от Shape.
2. Реализовать метод .area().
Например:
```python
class Rectangle(Shape):
    def __init__(self, width, height):
        self.width = width
        self.height = height

    def area(self):
        return self.width * self.height
```

## 📋 Тесты
| № | Название теста                    | Что проверяет                                                                                          | Ожидаемое поведение                                           | Результат |
| - | --------------------------------- | ------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------- | :-: |
| 1 | **`test_circle_area`**            | Проверяет правильность вычисления площади круга по формуле `πr²`                                       | При радиусе 1 площадь ≈ 3.14159 (`math.pi`)                   | ✅ |
| 2 | **`test_triangle_area`**          | Проверяет правильность вычисления площади треугольника по трём сторонам (формула Герона)               | Для сторон 3, 4, 5 площадь = 6.0                              | ✅ |
| 3 | **`test_right_triangle`**         | Проверяет метод `is_right()` — определяет, является ли треугольник прямоугольным                       | Для сторон 3, 4, 5 → `True`                                   | ✅ |
| 4 | **`test_non_right_triangle`**     | Проверяет, что треугольник 2, 3, 4 **не** прямоугольный                                                | Для сторон 2, 3, 4 → `False`                                  | ✅ |
| 5 | **`test_calculate_area_dynamic`** | Проверяет, что функция `calculate_area()` вычисляет площадь без знания типа фигуры (через полиморфизм) | Для круга радиусом 2 → `4π`, для треугольника 3, 4, 5 → `6.0` | ✅ |
| 6 | **`test_invalid_circle`**         | Проверяет, что при отрицательном радиусе вызывается ошибка `ValueError`                                | `Circle(-1)` → исключение                                     | ✅ |
| 7 | **`test_invalid_triangle`**       | Проверяет валидацию треугольника — если сумма двух сторон ≤ третьей, он не существует                  | `Triangle(1, 2, 3)` → исключение                              | ✅ | 

<img width="1091" height="398" alt="image" src="https://github.com/user-attachments/assets/b40660f0-cf05-4cab-85b9-8633f75a6468" />

